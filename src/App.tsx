import React from 'react';
import IndexRouter from './page';

type Props = {};

const App = (props: Props) => {
  /* Router */
  /* State */
  /* Functions */
  /* Hooks */
  /* Render */
  return <IndexRouter />;
};

export default App;
